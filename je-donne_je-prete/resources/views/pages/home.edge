<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Je donne, je prête – Connexion</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-black text-white min-h-screen flex flex-col items-center justify-center font-sans">
    <!-- Logo / Titre -->
    <div class="text-center mb-10">
      <h1 class="text-5xl font-bold mb-4">Je donne, je prête</h1>
      <p class="text-gray-300 text-lg">
        Connecte-toi pour donner ou emprunter des objets gratuitement
      </p>
    </div>

    <!-- Formulaire -->
    <form action="/login" method="POST" class="w-80 flex flex-col gap-4">
      <!-- Champ User -->
      <input
        type="User"
        name="User"
        placeholder="User"
        class="w-full px-4 py-3 rounded-md bg-zinc-900 border border-zinc-700 focus:outline-none focus:border-white"
        required
      />

      <!-- Champ mot de passe -->
      <input
        type="password"
        name="password"
        placeholder="Mot de passe"
        class="w-full px-4 py-3 rounded-md bg-zinc-900 border border-zinc-700 focus:outline-none focus:border-white"
        required
      />

      <!-- Bouton de connexion -->
      <button
        type="submit"
        class="bg-white text-black font-semibold py-3 rounded-md hover:bg-gray-200 transition-colors"
      >
        Se connecter
      </button>
    </form>

    
  </body>
</html>
